<thead>
  <tr>
    {{#each columnComponents as |column|}}
      {{component column.headerComponentName column=column}}
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each content as |row|}}
    <tr>
      {{yield row}}
    </tr>
  {{/each}}
</tbody>
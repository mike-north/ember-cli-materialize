import Ember from 'ember';
import layout from '../templates/components/materialize-footer';

export default Ember.Component.extend({
  layout: layout,
  tagName: 'footer',
  classNames: ['page-footer']
});

import Ember from 'ember';
import layout from '../templates/components/md-fab';

export default Ember.Component.extend({
  classNames: ['fixed-action-btn'],
  attributeBindings: ['style'],
  layout
});

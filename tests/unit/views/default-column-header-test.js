import { moduleFor, test } from 'ember-qunit';

moduleFor('view:default-column-header', 'Unit | View | default column header');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
